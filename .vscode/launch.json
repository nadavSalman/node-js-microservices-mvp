{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/health-service/package.json",
                "localRoot": "${workspaceFolder}/health-service"
            }
        }
    ]
}